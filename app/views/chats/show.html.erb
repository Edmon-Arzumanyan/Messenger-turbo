<%= turbo_frame_tag "#{current_user.id}_show" do %>
  <%= render partial: "chats/show",
             locals: {
               user: current_user,
               messages: @messages,
               message: @message,
               chat: @chat,
             } %>
<% end %>
